<Window x:Class="VPSManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="350" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#202123"
        ShowInTaskbar="False">

    <Border BorderBrush="#3E3E42" BorderThickness="1">
        <StackPanel>
            <!-- Header -->
            <Border Background="#18181B" Padding="16,12">
                <TextBlock x:Name="TitleText" Text="Input"
                           FontSize="14" FontWeight="SemiBold" Foreground="#ECECF1"/>
            </Border>

            <!-- Form -->
            <StackPanel Margin="16,16,16,12">
                <TextBlock x:Name="LabelText" Text="Value:" Foreground="#ECECF1"
                           FontWeight="Medium" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="InputBox" Style="{StaticResource DarkTextBox}"/>
            </StackPanel>

            <!-- Buttons -->
            <Border Background="#18181B" Padding="16,12" BorderBrush="#3E3E42" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource SecondaryButton}" Content="Cancel"
                            Click="Cancel_Click" Padding="14,6" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="OK"
                            Click="OK_Click" Padding="14,6"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</Window>
